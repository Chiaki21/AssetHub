<UserControl x:Class="AssetHub.NotificationToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="400" Height="70" Margin="10">
    <Border Background="White" CornerRadius="8" BorderThickness="1" BorderBrush="#E2E8F0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
        </Border.Effect>
        <Grid Margin="15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:PackIcon x:Name="TypeIcon" 
                        Kind="CheckCircle" 
                        Foreground="#10B981" 
                        Width="24" Height="24" 
                        VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="TxtTitle" Text="Success" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock x:Name="TxtMessage" Text="Asset added successfully!" FontSize="12" Foreground="#64748B"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>