<UserControl x:Class="AssetHub.AssignAssetDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Width="400" Padding="30" Background="White">

    <StackPanel>
        <TextBlock Text="Assign Asset" FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>

        <TextBlock Text="Assign To" Foreground="#64748B" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbEmployees" 
                  DisplayMemberPath="FullName"
                  materialDesign:HintAssist.Hint="Choose an employee"
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                  Margin="0,0,0,20"/>

        <TextBlock Text="Available Asset" Foreground="#64748B" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbAssets" 
                  DisplayMemberPath="AssetName"
                  materialDesign:HintAssist.Hint="Choose an asset"
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                  Margin="0,0,0,30"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL" 
                    Click="Cancel_Click" 
                    Style="{StaticResource MaterialDesignFlatButton}" 
                    Foreground="#64748B" 
                    Margin="0,0,10,0"/>

            <Button Content="CONFIRM ASSIGNMENT" 
                    Click="Assign_Click" 
                    Background="#2563EB" 
                    Foreground="White" 
                    Height="40">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </StackPanel>
</UserControl>