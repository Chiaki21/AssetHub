<Window x:Class="AssetHub.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Employee" Height="550" Width="400" 
        WindowStartupLocation="CenterScreen" Background="#F8FAFC">
    <StackPanel Margin="30">
        <TextBlock Text="New Employee" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#1E293B"/>

        <TextBlock Text="Full Name" Foreground="#64748B" Margin="0,10,0,5"/>
        <TextBox x:Name="TxtName" Padding="10" Height="40" VerticalContentAlignment="Center"/>

        <TextBlock Text="Email Address" Foreground="#64748B" Margin="0,10,0,5"/>
        <TextBox x:Name="TxtEmail" Padding="10" Height="40" VerticalContentAlignment="Center"/>

        <TextBlock Text="Department" Foreground="#64748B" Margin="0,10,0,5"/>
        <ComboBox x:Name="CmbDept" Height="40" Padding="10,5">
            <ComboBoxItem Content="IT"/>
            <ComboBoxItem Content="HR"/>
            <ComboBoxItem Content="Finance"/>
            <ComboBoxItem Content="Marketing"/>
        </ComboBox>
        <TextBlock Text="Position / Job Title" Foreground="#64748B" Margin="0,10,0,5"/>
        <TextBox x:Name="TxtPosition" Padding="10" Height="40" VerticalContentAlignment="Center"/>

        <UniformGrid Columns="2" Margin="0,30,0,0">
            <Button Content="Cancel" IsCancel="True" Background="Transparent" BorderThickness="0"/>
            <Button Content="Save Employee" Click="BtnSave_Click" Background="#2563EB" Foreground="White" FontWeight="Bold" Height="40">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </UniformGrid>
    </StackPanel>
</Window>